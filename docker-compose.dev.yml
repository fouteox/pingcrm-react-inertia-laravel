services:
  app:
    build:
      target: development
      args:
        USER_ID: ${SPIN_USER_ID}
        GROUP_ID: ${SPIN_GROUP_ID}
    volumes:
      - .:/var/www/html/
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
      typesense:
        condition: service_started
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pingcrm.rule=Host(`${APP_HOST}`)"
      - "traefik.http.routers.pingcrm.entrypoints=websecure"
      - "traefik.http.routers.pingcrm.tls=true"
      - "traefik.http.services.pingcrm.loadbalancer.server.port=8000"

  queue:
    build:
      target: development
      args:
        USER_ID: ${SPIN_USER_ID}
        GROUP_ID: ${SPIN_GROUP_ID}
    volumes:
      - .:/var/www/html/
    depends_on:
      app:
        condition: service_started
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy

  scheduler:
    build:
      target: development
      args:
        USER_ID: ${SPIN_USER_ID}
        GROUP_ID: ${SPIN_GROUP_ID}
    volumes:
      - .:/var/www/html/
    depends_on:
      app:
        condition: service_started
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy

  reverb:
    build:
      target: development
      args:
        USER_ID: ${SPIN_USER_ID}
        GROUP_ID: ${SPIN_GROUP_ID}
    volumes:
      - .:/var/www/html/
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.reverb.rule=Host(`${APP_HOST}`) && (PathPrefix(`/app`) || PathPrefix(`/apps`))"
      - "traefik.http.routers.reverb.entrypoints=websecure"
      - "traefik.http.routers.reverb.tls=true"
      - "traefik.http.services.reverb.loadbalancer.server.port=8080"
    depends_on:
      app:
        condition: service_started
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy

  valkey:
    volumes:
      - ./.infrastructure/volume_data/valkey/valkey_data/:/data

  postgres:
    volumes:
      - ./.infrastructure/volume_data/postgres/database_data/:/var/lib/postgresql/data

  typesense:
    volumes:
      - ./.infrastructure/volume_data/typesense/typesense_data/:/data

  vite:
    build:
      target: development
      args:
        USER_ID: ${SPIN_USER_ID}
        GROUP_ID: ${SPIN_GROUP_ID}
    command: ["sh", "-c", "vp install && vp dev"]
    volumes:
      - .:/var/www/html/
      - vite-plus-home:/opt/vite-plus
    working_dir: /var/www/html/
    environment:
      - VITE_DEV_HOST=${VITE_DEV_HOST}
      - APP_HOST=${APP_HOST}
    networks:
      - internal
      - spin-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vite.rule=Host(`${VITE_DEV_HOST}`)"
      - "traefik.http.routers.vite.entrypoints=websecure"
      - "traefik.http.routers.vite.tls=true"
      - "traefik.http.services.vite.loadbalancer.server.port=5173"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5173/@vite/client"]
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 3
    depends_on:
      app:
        condition: service_started
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy

# Persist vite+'s managed Node runtime and caches across container recreations.
volumes:
  vite-plus-home:
